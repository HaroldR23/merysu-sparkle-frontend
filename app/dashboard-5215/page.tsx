'use client';

import { Sidebar } from "./components/Sidebar/Sidebar";

const Dashboard = () => {
  return (
    <>
      <Sidebar activeSection="" onNavigate={() => {}} isOpen={true} onClose={() => {}} />
    </>
  )
}

export default Dashboard;
